import React from 'react'
import { ScrollView, StyleSheet, Text, View } from 'react-native'

export const ReviewTransferScrollView = () => {
  return (
    <View>
      <Text></Text>
    </View>
  )
}

export default ReviewTransferScrollView

const styles = StyleSheet.create({})
