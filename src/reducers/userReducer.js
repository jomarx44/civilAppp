import {
  
} from "../actions/types";

const initialState = {
  accountsList: {},
  accounts: {},
  accountsById: [],
  form: {},
  status: {
    isCreating: false,
    isFetching: false
  }
}

export const userReducer = (state = initialState, action) => {
  switch(action.type) {
    case : 

    break;
  }
};

export default userReducer;